#
#
#	$Id: yMakefile,v 1.4 1992-09-09 15:07:17 clyne Exp $
#
#	Makefile for ncgmstat
#
#

NCARG = ../../../..

MYNAME	= ncgmstat

LIBS		= $(LIBPATH)/libcgm.a $(LIBPATH)/libncarg_c.a

CDEFINES	=

OBJS		= ncgmstat.o
SRCS		= ncgmstat.c

all::	$(MYNAME)

CTarget($(MYNAME),$(OBJS),$(LIBS))
MakefileTarget
CleanTarget

saber_src: $(SRCS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(SRCS) $(LIBS)

saber_obj: $(OBJS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(OBJS) $(LIBS)


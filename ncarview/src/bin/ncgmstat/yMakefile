#
#
#	$Id: yMakefile,v 1.6 1993-10-19 17:43:20 boote Exp $
#
#	Makefile for ncgmstat
#
#

MYNAME	= ncgmstat

PROG_LIBS	=	$(CGMLIB) $(NCARGCLIB)
DEP_LIBS	=	$(DEPCGMLIB) $(DEPNCARGCLIB)

OBJS		= ncgmstat.o
SRCS		= ncgmstat.c

CProgram($(MYNAME),$(OBJS),$(DEP_LIBS))
DependTarget($(SRCS))

saber_src: $(SRCS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(SRCS) $(LIBS)

saber_obj: $(OBJS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(OBJS) $(LIBS)

#
#
#	$Id: yMakefile,v 1.2 1992-06-25 21:48:09 clyne Exp $
#
#
#	Makefile for rasstat
#
#

NCARG = ../../../..
BINDIR_LOCAL = ../../../bin

MYNAME	= rasstat

LOCLIBDIR	= ../../../lib

LIBS		= $(LIBDIR)/libncarg_ras.a $(LIBDIR)/libhdf.a \
		$(LIBDIR)/libncarv.a -lm

CDEFINES	= 

OBJS		= rasstat.o
SRCS		= rasstat.c

all::	$(MYNAME)

CTarget($(MYNAME),$(OBJS),$(LIBS))
MakefileTarget
CleanTarget

saber_src: $(SRCS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(SRCS) $(LIBS)

saber_obj: $(OBJS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(OBJS) $(LIBS)


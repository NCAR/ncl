#
#	This script simply reports the libraries required by ctrans.
#	The library list will vary depending on machine configuration
#
#!/bin/csh

echo /usr/local/lib/libictrans.o /usr/local/lib/libctrans.o  /usr/local/lib/libcgm.a /usr/local/lib/libncarv.a -lX11  -lsuntool -lsunwindow -lpixrect -lm

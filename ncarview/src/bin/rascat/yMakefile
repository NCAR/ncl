#
#
#	$Id: yMakefile,v 1.10 1992-09-25 16:21:01 clyne Exp $
#
#
#	Makefile for rascat
#
#

NCARG = ../../../..

MYNAME		= rascat

LIBS		= $(LIBPATH)/libncarg_ras.a \
		$(LIBPATH)/libncarg_c.a $(HDFLIB) -lm

CDEFINES	= 

OBJS		= rascat.o
SRCS		= rascat.c

all::	$(MYNAME)

CTarget($(MYNAME),$(OBJS),$(LIBS))
MakefileTarget
CleanTarget

saber_src: $(SRCS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(SRCS) $(LIBS)

saber_obj: $(OBJS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(OBJS) $(LIBS)


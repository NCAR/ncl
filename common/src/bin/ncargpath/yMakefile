#
#
#	$Id: yMakefile,v 1.1 1992-09-09 17:37:48 clyne Exp $
#
#
#	Makefile for ncargpath
#
#

NCARG = ../../../..

MYNAME		= ncargpath

LIBS		= $(LIBPATH)/libncarg_c.a 

CDEFINES	= 

OBJS		= ncargpath.o
SRCS		= ncargpath.c

all::	$(MYNAME)

CTarget($(MYNAME),$(OBJS),$(LIBS))
MakefileTarget
CleanTarget

saber_src: $(SRCS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(SRCS) $(LIBS)

saber_obj: $(OBJS)
	#setopt load_flags $(CFLAGS) $(CINCS)
	#load $(OBJS) $(LIBS)


/*
 *      $Id: Site,v 1.5 1994-03-10 00:54:51 boote Exp $
 *
 ************************************************************************
 *									*
 *			   Copyright (C)  1993				*
 *	     University Corporation for Atmospheric Research		*
 *			   All Rights Reserved				*
 *									*
 ************************************************************************
 *
 *	File:		Site
 *
 *	Author:		Jeff W. Boote
 *			National Center for Atmospheric Research
 *			PO 3000, Boulder, Colorado
 *
 *	Date:		Fri Apr 30 14:16:46 MDT 1993
 *
 *	Description:	This file contains any site over-ride directives
 *			such as which part's of the raster library to
 *			build etc...
 *
 *			This file contains two halves.  The first half,
 *			contained in the #ifdef FirstSite...#endif should
 *			only set things that the "SystemIncludes" files
 *			depend upon.  The second half, contained in the
 *			#ifdef SecondSite...#endif should contain other
 *			definitions.
 */

#ifdef FirstSite
/* #define TryCodeCenter */
#endif /* FirstSite */

#ifdef SecondSite

/*
 * Set YmakeRoot and others if needed - config.perl should eventually
 * set these things here instead of in each config file.
 * If these are not set the default is /usr/local for NcargRoot and
 * all the others are sub-dirs of that.
 * The NcargVersion variable is defaulted in Project.
 */

#define YmakeRoot /usr/local/ncarg
#define	InstallYmake

/* #define BinRoot /usr/local/bin */
/* #define LibRoot /usr/local/lib */
/* #define IncRoot /usr/local/include */
/* #define ManRoot /usr/local/man */

/*
 * Again the configure script should edit these variables
 */
/* #define TmpRoot /tmp */

#define LibSearch -L/X/X11/lib -L/usr/local/lib 
#define IncSearch -I/X/X11/include -I/usr/local/include

#ifdef	hostshavano
#define	BuildNCARGSourceLibrary
#endif

#endif /* SecondSite */

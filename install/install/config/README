This directory contains machine specific command/configurations. The env.*
files specify the incantations for various commands. The paths.*
files contain the default intallation directories for NCAR G modules.
